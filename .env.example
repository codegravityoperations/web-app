# API base URL for local development and deployed environments
# For local dev, Vite proxies /api/* requests to this endpoint.
# The backend expects the full path, so include /api in the URL.
VITE_API_BASE_URL=http://my-backend-alb-416448329.us-east-1.elb.amazonaws.com/api
